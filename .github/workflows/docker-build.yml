name: Build & Deploy Wisecow

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Checkout code
    - name: Checkout repository
      uses: actions/checkout@v3

    # 2️⃣ Set up Docker Buildx
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    # 3️⃣ Login to Docker Hub
    - name: Docker login
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # 4️⃣ Build & Push Docker image
    - name: Build & Push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: sabarisurya/wisecow:latest

    # 5️⃣ Install kubectl
    - name: Install kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'v1.27.4'

    # 6️⃣ Install kind manually (avoids json5 errors)
    - name: Install kind
      run: |
        curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.20.0/kind-linux-amd64
        chmod +x ./kind
        sudo mv ./kind /usr/local/bin/kind

    # 7️⃣ Create kind cluster
    - name: Create kind cluster
      run: kind create cluster --name wisecow-cluster

    # 8️⃣ Load Docker image into kind
    - name: Load Docker image into kind
      run: kind load docker-image sabarisurya/wisecow:latest --name wisecow-cluster

    # 9️⃣ Deploy Wisecow to Kubernetes
    - name: Deploy Wisecow manifests
      run: |
        # Create TLS secret if not exists
        kubectl create secret tls wisecow-tls --cert=tls.crt --key=tls.key || true
        
        # Apply manifests
        kubectl apply -f wisecow-deployment.yaml
        kubectl apply -f wisecow-service.yaml
        kubectl apply -f wisecow-ingress.yaml || true
        
        # Update deployment image
        kubectl set image deployment/wisecow wisecow=sabarisurya/wisecow:latest --record

    # 🔟 Verify deployment
    - name: Verify deployment
      run: kubectl get pods -o wide
